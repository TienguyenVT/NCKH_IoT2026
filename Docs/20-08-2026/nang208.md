CHỌN LINH KIỆN VÀ CHỌN LỐI ĐI 
Vi điều khiển : ESP32 S3
mppt sử dụng tinylm
*sử dụng mermaid để biểu đồ có nhánh
#cách tạo nhánh mới trên github :
-Vào github, ấn vào new branch. 
-Ấn vào nhánh của mình , ấn vào  pull request.
-Cach gop nhánh vào main: ấn vào icon dưới cột pull request, lướt xuống dưới cùng bấm vào merge pull requets rồi ấn confirm
