1. tai git? 
2. dung git version
3. tai link + power shell
4. đẩy code = terminal: 
trước khi add thì vào git fetch để cập nhật, sau là git pull origin main, tiếp theo là git add
git commit -m "test commit"
git push origin main
(loi thi dung git pull để kéo code hoặc git fetch)
5. git commit -m 
6. tạo nhánh brach trên github và quy tắc đặt tên
7. tạo pull request để đẩy lên main 
8. git checkout -b feature/taskA để tạo nhánh mới, nhớ git fetch để cập nhật
9. git push tên nhánh để cập trên github
10. sau khi dc request chấp nhận , git fetch sau đó git pull origin main để dc cập nhật vào main chính