1. Cách clone code từ github
+Tải git
+mở powershell 
+lấy link http từ trên gỉthub
+cd sang folder cần
+gõ lệnh : git clone (link cần tải)
*git version để check  phiên bản vs code
**:Tìm cách working tree 
2. đẩy code
trước khi add thì git fetch là cập nhật, sau fetch là git pull origin main, tiếp theo là git add.
git commit -m "test commit"
git push origin (ten nhanh)
iwgigfigefig
